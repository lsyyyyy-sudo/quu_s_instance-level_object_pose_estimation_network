# ============================================================
#  大文件 / 原始数据 / 第三方代码 —— 一律不入库
#  说明见 README.md 的「数据与参考资料」一节
# ============================================================

# 目标数据视频（277 MB，超过 GitHub 单文件 100 MB 硬限制）
head_left_rgb_raw.mp4(1)/
head_left_rgb_raw*.mp4
__MACOSX/

# 第三方论文与代码克隆（arXiv PDF + BoxDreamer + HCCEPose，共约 250 MB）
refs/

# 内部资料：考核题目原文（含内部链接，仅本地保留，不入库）
docs/ASSIGNMENT.md

# 人类自己的实验笔记（本文件由人类所有，仅本地保留）
# ⚠️ 它和下面那些截图**曾经被误提交并公开**过（见 docs/TROUBLESHOOTING.md 的 GIT-02）。
#    已从当前版本移出（git rm --cached）；公开历史里仍有旧副本，但已确认内容不敏感，
#    故不做历史重写。加规则是为了**不再复发** —— 否则下一次 `git add .` 会原样带回来。
#    自动检查见 scripts/check_repo.py（第 3 项：敏感文件是否被跟踪）。
docs/HUMAN_LOG.md

# 个人截图工具产出的图（联想截图 / Snipaste / 微信 / QQ 等），一律不入库
*截图*.png
*截图*.jpg
Snipaste*.png
微信截图*.png
QQ图片*.png

# ============================================================
#  Python
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 虚拟环境（本项目用多个隔离环境：训练 .venv、3D 生成 .venv-3d ...）
/.venv*/
/venv/
/ENV/
/env/
.env
.env.*
!.env.example
.python-version

# 测试 / 类型检查 / 格式化缓存
.pytest_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.cache
.mypy_cache/
.dmypy.json
.ruff_cache/
.hypothesis/

# ============================================================
#  深度学习：权重、日志、训练产物
# ============================================================
*.pth
*.pt
*.ckpt
*.onnx
*.engine
*.pkl
*.h5
*.safetensors

# 权重 / 日志目录：同样必须锚定到根目录，否则会误伤任意深度的同名源码目录
# （见下面「数据集 / 渲染产物」一节的说明和 docs/TROUBLESHOOTING.md 的 GIT-03）
/checkpoints/
/weights/
/pretrained/
/lightning_logs/
/wandb/
/runs/
/logs/
*.log
events.out.tfevents.*

# ============================================================
#  数据集 / 渲染产物 / 推理输出
# ============================================================
# ⚠️ 这些模式**必须锚定到仓库根目录**（前导 `/`）。
# 无锚点的 `datasets/` 会匹配**任意深度**的同名目录，
# 把源码目录 `src/datasets/` 也一起忽略掉——本项目真的踩过这个坑，
# 导致 src/datasets/ 整个包从来没被提交。详见 docs/TROUBLESHOOTING.md 的 GIT-03。
/data/
/datasets/
/dataset/

# 渲染产物：这两类允许出现在任意深度（它们本来就在 data/ 下面）
train_pbr/
test_pbr/

# 训练 / 推理输出（同样锚定到根目录）
/outputs/
/output/
/results/
/preds/

# ============================================================
#  编辑器 / 系统
# ============================================================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
